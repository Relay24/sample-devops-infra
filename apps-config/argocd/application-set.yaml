apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: all-apps
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - git: # Generator for local apps configuration files
              repoURL: 'https://github.com/your-org/apps-config.git'
              revision: HEAD
              directories:
                - path: helm-apps/*
                  metadata:
                    labels:
                      app-type: helm
                - path: yaml-apps/*
                  metadata:
                    labels:
                      app-type: yaml
          - git:  # Generator for external apps configuration files
              repoURL: 'https://github.com/your-org/apps-config.git'
              revision: HEAD
              files:
                - path: "external-apps/*.yaml"

  template:
    metadata:
      name: '{{appName | default(path.basename)}}'
      labels:
        app-type: '{{appType | default(path.basename)}}'
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        targetRevision: '{{revision}}'
        path: '{{path}}'
        helm:
          valueFiles:
            - '{{helmValuesFile | default("")}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: '{{appNamespace | default(path.basename)}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true